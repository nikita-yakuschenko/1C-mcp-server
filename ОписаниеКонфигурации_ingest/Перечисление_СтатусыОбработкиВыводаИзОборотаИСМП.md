# Перечисление: СтатусыОбработкиВыводаИзОборотаИСМП (Статусы обработки вывода из оборота ИСМП)

## Значения

- Черновик (Черновик)
- КПередаче (К выводу из оборота)
- ОшибкаПередачи (К выводу из оборота (ошибка))
- Обрабатывается (К выводу из оборота (обрабатывается))
- КодыМаркировкиВыведеныИзОборота (Коды маркировки выведены из оборота)
- АннулированиеВыводаИзОборотаКПередаче (К аннулированию)
- АннулированиеВыводаИзОборотаОбрабатывается (К аннулированию (обрабатывается))
- АннулированиеВыводаИзОборотаОшибка (К аннулированию (ошибка))
- Аннулирован (Аннулирован)

