# Перечисление: СтатусыОбработкиТТНИсходящейЕГАИС (Статусы обработки ТТН ЕГАИС (исходящей))

## Значения

- Черновик (Черновик)
- КПередаче (К передаче)
- ПереданВУТМ (Передан в УТМ)
- ОбрабатываетсяЕГАИС (Обрабатывается ЕГАИС)
- ПередаетсяАктОтказа (Передается акт отказа)
- ПереданАктОтказа (Передан акт отказа)
- ОшибкаПередачиАктаОтказа (Ошибка передачи акта отказа)
- ОбрабатываетсяКлиентом (Обрабатывается клиентом)
- ОшибкаПередачи (Ошибка передачи в ЕГАИС)
- АктОтказаКПередаче (К отмене)
- АктОтказаПереданВУТМ (К отмене (передан в УТМ))
- АктОтказаОбрабатываетсяЕГАИС (К отмене (обрабатывается ЕГАИС))
- АктОтказаОшибка (К отмене (ошибка))
- Подтвержден (Подтвержден)
- ОтмененКлиентом (Отменен клиентом)
- АктРасхожденийПринят (Принят акт расхождений)
- АктРасхожденийПодтверждениеКПередаче (К подтверждению)
- АктРасхожденийПодтверждениеПереданВУТМ (К подтверждению (передан в УТМ))
- АктРасхожденийПодтверждениеОбрабатываетсяЕГАИС (К подтверждению (обрабатывается ЕГАИС))
- АктРасхожденийПодтверждениеОшибка (К подтверждению (ошибка))
- АктРасхожденийОтказКПередаче (К отмене)
- АктРасхожденийОтказПереданВУТМ (К отмене (передан в УТМ))
- АктРасхожденийОтказОбрабатываетсяЕГАИС (К отмене (обрабатывается ЕГАИС))
- АктРасхожденийОтказОшибка (К отмене (ошибка))
- ПодтвержденСРасхождениями (Подтвержден*)
- ЗапросНаОтменуПроведенияПринят (Принят запрос на отмену проведения)
- ЗапросНаОтменуПроведенияПодтверждениеКПередаче (К отмене проведения)
- ЗапросНаОтменуПроведенияПодтверждениеПереданВУТМ (К отмене проведения (передан в УТМ))
- ЗапросНаОтменуПроведенияПодтверждениеОбрабатываетсяЕГАИС (К отмене проведения (обрабатывается ЕГАИС))
- ЗапросНаОтменуПроведенияПодтверждениеОшибка (К отмене проведения (ошибка))
- ЗапросНаОтменуПроведенияОтказКПередаче (Отказ отмены проведения)
- ЗапросНаОтменуПроведенияОтказПереданВУТМ (Отказ отмены проведения (передан в УТМ))
- ЗапросНаОтменуПроведенияОтказОбрабатываетсяЕГАИС (Отказ отмены проведения (обрабатывается ЕГАИС))
- Отменен (Отменен)
- ЗапросНаОтменуПроведенияОтказОшибка (Отказ отмены проведения (ошибка))

