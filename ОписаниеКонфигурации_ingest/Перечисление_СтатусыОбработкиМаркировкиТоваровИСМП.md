# Перечисление: СтатусыОбработкиМаркировкиТоваровИСМП (Статусы обработки маркировки товаров ИСМП)

## Значения

- Черновик (Черновик)
- ВводВОборотКПередаче (К вводу в оборот)
- ВводВОборотОбрабатывается (К вводу в оборот (обрабатывается))
- ВводВОборотОшибкаПередачи (К вводу в оборот (ошибка))
- КодыМаркировкиВведеныВОборот (Коды маркировки введены в оборот)
- ОтчетКПередаче (К отчету о нанесении)
- ОтчетОбрабатывается (К отчету о нанесении (обрабатывается))
- ОтчетОшибкаПередачи (К отчету о нанесении (ошибка))
- КодыМаркировкиНанесены (Коды маркировки нанесены)
- КодыМаркировкиНанесеныЧастично (Коды маркировки нанесены частично)
- АгрегацияКПередаче (К агрегации)
- АгрегацияОбрабатывается (К агрегации (обрабатывается))
- АгрегацияОшибкаПередачи (К агрегации (ошибка))
- КодыМаркировкиАгрегированыЧастично (Коды маркировки агрегированы частично)
- КодыМаркировкиАгрегированы (Коды маркировки агрегированы)
- КодыМаркировкиНанесеныИАгрегированы (Коды маркировки нанесены и агрегированы)
- КодыМаркировкиВведеныВОборотИАгрегированы (Коды маркировки введены в оборот и агрегированы)
- КодыМаркировкиВведеныВОборотИСформированыАТК (Коды маркировки введены в оборот и сформированы АТК)
- ФормированиеАТККПередаче (К формированию АТК)
- ФормированиеАТКОбрабатывается (К формированию АТК (обрабатывается))
- ФормированиеАТКОшибкаПередачи (К формированию АТК (ошибка передачи))
- КодыАТКСформированы (Коды АТК сформированы)
- КодыМаркировкиВведеныВОборотЧастично (Коды маркировки введены в оборот частично)
- ОтчетСтатусНеопределен (Статус отчета неопределен)
- ПодтверждениеПоступленияКИЗКПередаче (К подтверждению поступления КИЗ)
- ПодтверждениеПоступленияКИЗОбрабатывается (К подтверждению поступления КИЗ (обрабатывается))
- ПодтверждениеПоступленияКИЗОшибкаПередачи (К подтверждению поступления КИЗ (ошибка))
- ПоступлениеКИЗПодтверждено (Поступление КИЗ подтверждено)
- ПоступлениеКИЗПодтвержденоЧастично (Поступление КИЗ подтверждено частично)

