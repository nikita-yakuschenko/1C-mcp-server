# Перечисление: СтатусыОбработкиТТНВходящейЕГАИС (Статусы обработки ТТН ЕГАИС (входящей))

## Значения

- ПринятИзЕГАИС (Принят из ЕГАИС)
- АктПодтвержденияКПередаче (К подтверждению)
- АктПодтвержденияПереданВУТМ (К подтверждению (передан в УТМ))
- АктПодтвержденияОбрабатываетсяЕГАИС (К подтверждению (обрабатывается ЕГАИС))
- АктПодтвержденияОшибка (К подтверждению (ошибка))
- Подтвержден (Подтвержден)
- АктОтказаКПередаче (К отмене)
- АктОтказаПереданВУТМ (К отмене (передан в УТМ))
- АктОтказаОбрабатываетсяЕГАИС (К отмене (обрабатывается ЕГАИС))
- АктОтказаОшибка (К отмене (ошибка))
- АктРасхожденийКПередаче (К подтверждению*)
- АктРасхожденийПереданВУТМ (К подтверждению* (передан в УТМ))
- ПередаетсяЗапросНаОтменуПроведения (Передается запрос на отмену проведения)
- ПереданЗапросНаОтменуПроведения (Передан запрос на отмену проведения)
- ОшибкаПередачиЗапросаНаОтменуПроведения (Ошибка передачи запроса на отмену проведения)
- АктРасхожденийОбрабатываетсяЕГАИС (К подтверждению* (обрабатывается ЕГАИС))
- АктРасхожденийОшибка (К подтверждению* (ошибка))
- ПодтвержденСРасхождениями (Подтвержден*)
- ОтмененПоставщиком (Отменен поставщиком)
- ЗапросНаОтменуПроведенияКПередаче (К отмене проведения)
- ЗапросНаОтменуПроведенияПереданВУТМ (К отмене проведения (передан в УТМ))
- ЗапросНаОтменуПроведенияОбрабатываетсяЕГАИС (К отмене проведения (обрабатывается ЕГАИС))
- Отменен (Отменен)
- ЗапросНаОтменуПроведенияОшибка (К отмене проведения (ошибка))
- ЗапросНаОтменуАктаОтказаКПередаче (К отмене акта отказа)
- ЗапросНаОтменуАктаОтказаПереданВУТМ (К отмене акта отказа (передан в УТМ))
- ЗапросНаОтменуАктаОтказаОбрабатываетсяЕГАИС (К отмене акта отказа (обрабатывается ЕГАИС))
- ЗапросНаОтменуАктаОтказаОшибка (К отмене акта отказа (ошибка))

