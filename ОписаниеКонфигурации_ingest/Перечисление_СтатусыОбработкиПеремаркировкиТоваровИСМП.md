# Перечисление: СтатусыОбработкиПеремаркировкиТоваровИСМП (Статусы обработки перемаркировки ИСМП)

## Значения

- Черновик (Черновик)
- БлокировкаКодовМаркировкиКПередаче (К блокировке кодов маркировки)
- БлокировкаКодовМаркировкиОбрабатывается (К блокировке кодов маркировки (обрабатывается))
- БлокировкаКодовМаркировкиОшибкаПередачи (К блокировке кодов маркировки (ошибка))
- КодыМаркировкиЗаблокированыДляПродажи (Коды маркировки заблокированы для продажи)
- ПеремаркировкаКПередаче (К перемаркировке)
- ПеремаркировкаОбрабатывается (К перемаркировке (обрабатывается))
- ПеремаркировкаОшибкаПередачи (К перемаркировке (ошибка))
- ПеремаркировкаВыполнена (Перемаркировка выполнена)
- ИндивидуализацияКИЗКПередаче (К индивидуализации КИЗ)
- ИндивидуализацияКИЗОбрабатывается (К индивидуализации КИЗ (обрабатывается))
- ИндивидуализацияКИЗОшибкаПередачи (К индивидуализации КИЗ (ошибка))
- ИндивидуализацияКИЗВыполнена (Индивидуализация КИЗ (выполнена))

